{{- range .Values.configmaps }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .name }}
  namespace: {{ $.Release.Namespace }}
data:
{{- range $key, $value := .data }}
  {{ $key }}: {{ $value | toString | quote }}
{{- end }}  
{{- end }}